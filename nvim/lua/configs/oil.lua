return {
  watch_for_changes = true,
  view_options = {
    show_hidden_files = true, -- Show hidden files by default
  },
  float = {
    padding = 4,
    max_width = 0.8, -- Max width of the floating window
    max_height = 0.8, -- Max height of the floating window
  },
}
